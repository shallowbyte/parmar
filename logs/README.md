# `logs/` — run provenance

Raw transcripts, kept so the published numbers can be traced back to the run that produced them rather than taken on trust. Nothing here is an input to anything; all of it is a record.

| file | lines | size | what it records |
|---|---:|---:|---|
| `build_corpus.log` | 100 | 6.9 KB | Corpus construction transcript for the 256MB/1GB/4GB tiers, including fetch throughput and the per-tier checkpoint checksums. |
| `sweep_run.log` | 1,271 | 93.1 KB | Full console transcript of the sweep programme: every cell, its ratio, throughput, peak RSS and wall time, plus the per-stage validity drop log and pre-flight estimates. |
| `verify_boundaries.log` | 57 | 3.8 KB | Output of the blocking chunk-boundary differential test: whole-stream tokenization versus split-and-concatenated, for all four tokenizers at 1/2/4 MB chunks. **This is the evidence for the gate** — the sweep was not allowed to run until every combination came back token-identical with zero unsafe cuts. |

Absolute paths were rewritten out of these before publication; they contain no operator home directories.

<!-- generated by folder_docs.py -- edit that script, not this file -->
